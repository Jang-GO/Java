import java.util.*;

class Main{
    public int[] solution(int S,int N,int[] arr){
        int[] cache = new int[S];
        for(int x:arr){
            int idx = -1;
            for(int i=0;i<S;i++) if(cache[i]==x) idx = i;
            if(idx==-1){
                for(int i=S-1;i>=1;i--){
                    cache[i] = cache[i-1];
                }
            }else{
                for(int i=idx;i>=1;i--){
                    cache[i] = cache[i-1];
                }
            }
            cache[0]=x;
        }
        return cache;
    }
    public static void main(String[] args){
        Main T = new Main();
        Scanner sc = new Scanner(System.in);
        int S = sc.nextInt();
        int N = sc.nextInt();
        int[] arr = new int[N];
        for(int i=0;i<N;i++){
            arr[i] = sc.nextInt();
        }
        for(int x:T.solution(S,N, arr)){
           System.out.print(x+" ");
        }
    }
}   
