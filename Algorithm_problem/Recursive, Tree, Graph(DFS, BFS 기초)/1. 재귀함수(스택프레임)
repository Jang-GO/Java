import java.util.*;
class Main{
    public void DFS(int n){
        //보통 if-else문으로 if문에 종료조건 , else문에 실행문 적음 
        if(n==0) return; // return;은 종료의 의미(반환형이 void지만 return; 을쓰면 함수종료의 의미도 있다) 
        else{
            DFS(n-1);
            System.out.print(n+" "); 
        }
    }

    public static void main(String[] args){
        Main T = new Main();
        Scanner sc = new Scanner(System.in);
        int n= sc.nextInt();
        T.DFS(n);
    }
}
    
