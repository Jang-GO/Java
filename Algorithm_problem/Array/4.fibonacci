/*
 * 재귀와 for문 성능 for>재귀 -> 재귀는 스택프레임에 계속push
 */

import java.util.*;
class Main{
    static int[] fibo; // 예를들어 DFS(10) 이면 과정에서
    // DFS(9~1)까지 전부 값을 구하게됨. 따라서 그 값을 배열에 저장(기록)
    public int DFS(int n){
        if(fibo[n]>0) return fibo[n]; // 메모이제이션
        if(n==1) return fibo[n]=1;
        else if(n==2) return fibo[n]=1;
        else return fibo[n]=DFS(n-2)+DFS(n-1);

    }
    public static void main(String[] args){
        Main T = new Main();
        Scanner sc = new Scanner(System.in);
        int n= sc.nextInt();
        fibo = new int[n+1];
        T.DFS(n);
        for(int i=1;i<=n;i++){
            System.out.print(fibo[i]+" ");
        }
    }
}
/* 메모이제이션(시간 대폭감소)
 * = 이미 구한 DFS(x) 값을 다시검색할때는
 *   fibo[x] 값 리턴
 */  
    
